[build]
  command = """
    npm install process path-browserify stream-browserify buffer url --save-dev && \
    npm install --legacy-peer-deps && \
    npm run build
  """
  publish = "build"

[build.environment]
  NODE_VERSION = "18.20.4"
  NPM_VERSION = "10.8.1"
  NODE_ENV = "production"

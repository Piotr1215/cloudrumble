[build]
  command = """
    rm -rf node_modules && \
    npm install process --save && \
    npm install --legacy-peer-deps && \
    npm run build
  """
  publish = "build"

[build.environment]
  NODE_VERSION = "18.20.4"
  NPM_VERSION = "10.8.1"
  NODE_ENV = "production"
